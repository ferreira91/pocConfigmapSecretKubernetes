apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poc-configmap-secrets.fullname" . }}-test-connection"
  labels:
    {{- include "poc-configmap-secrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poc-configmap-secrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
